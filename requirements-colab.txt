# Google Colab / standard Python 3.12 environment.
# Do NOT install tensorflow here: Colab ships its own TensorFlow build.
# Reinstalling it in a running runtime can cause native .so symbol errors.
numpy
opencv-python-headless
pillow
pyyaml
matplotlib
tqdm
pandas
tomli; python_version < "3.11"
